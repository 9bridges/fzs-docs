---
sidebar_position: 6
---

# 创建数据链路

### 功能概述

将数据链路配置下发至 FZS Agent，初始化同步进程。创建成功后，链路状态从「未初始」变为「闲置」，等待启动。

:::tip
创建是启动链路前的必要步骤。创建只是初始化进程，不会立即开始数据同步。
:::

### 操作步骤

1. 在数据链路详情界面，点击链路操作，点击菜单中的 `创建` 按钮。

2. 等待创建完成后，「创建」按钮将置灰，链路状态变为 `闲置`。
